{
  "name": "boilplate-meta",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "check:docs": "tsx scripts/check-docs.ts",
    "realtime:parity": "tsx scripts/check-realtime-parity.ts",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run check:docs && npm run realtime:parity"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "tsx": "^4.19.4",
    "typescript": "~5.8.3"
  },
  "engines": {
    "node": ">=22"
  },
  "description": "Entry point, shared policy and tooling for the boilerplate ecosystem."
}
